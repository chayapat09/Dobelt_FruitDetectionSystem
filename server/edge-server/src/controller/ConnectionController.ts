// Received connection of Camera , Dobot , Belt and Combined to One System then add to memory
// If one disconnected disabled all new input ? 
// set all state to default function constructor ?
// All constructor have to ensure all state to default 
import EventEmitter from "node:events";

class ConnectionController extends EventEmitter {
    
}

